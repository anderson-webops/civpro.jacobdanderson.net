# Civ Pro 0.4.1: pilot preparation

This prerelease prepares instructor/student playtests and fixes problems reproduced in an agent rehearsal. Human sessions have not occurred: participants were unavailable on 2026-09-12. Pacing, free-exchange balance, and instructional effectiveness require human observation before promotion to a completed pilot evaluation.

- Preserve uncommitted reasoning through settings changes and autosave recovery. Keep form errors beside writing and move keyboard focus to each learning stage.
- Distinguish intentional nonresponse from an expired timer. Leaving Study mode or No timer starts a fresh window for the current timed step.
- Add one-for-one, budget-neutral exchanges so a full hand does not make required tools inaccessible. Record exchanges separately from played cards.
- Limit persistent balance history to numeric outcomes and canonical game labels. Scrub legacy stats and provide shared-browser cleanup for saves, drafts, and statistics. Downloaded replays still require separate deletion.
- Supply the pilot protocol, blank private-observation template, interim evaluation, and updated instructor/student guidance under `docs/pilot/` and the existing public guides.

Validation: clean dependency installation and full/production dependency audits on Node 24.18.1 with npm 12.0.2; zero reported vulnerabilities; full `npm run check`; 51 rule tests; source, scenario, replay, privacy, and public-artifact checks; desktop/mobile automated accessibility and keyboard flow. The new synthetic rehearsal completed 45 assessed rounds over all 15 existing packs, including all planned cooperative rounds and early-close Rule 56 paths. It used 242 exchanges under its deterministic search policy. Those results demonstrate tested reachability, not student pacing or game balance. The staged redacted secret scan found no leaks.

Identity: application/package/scenario release 0.4.1, content 2026-08-27.1, source review 2026-08-27. Compatible 0.4.0 snapshots remain accepted. No new doctrine review is claimed.

The client archive contains only the verified 22-file `dist/client` allowlist. `SHA256SUMS` records its checksum. This is source/artifact delivery; no production deployment was performed. The active pilot objective still requires real instructor/student sessions, improvements supported by their observations, and a final reviewed aggregate evaluation.
